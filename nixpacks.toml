[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npx vite build --config vite.railway.config.ts"]

[start]
cmd = "node railway-server.mjs"
